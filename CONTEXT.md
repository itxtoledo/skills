# Skills Collection

A collection of agent skills (slash commands and behaviors) loaded by AI coding agents.

## Language

**mocker**:
Docker-compatible CLI tool that runs on Apple's native Containerization framework (macOS 26+). Replaces `docker` in shell commands transparently.
_Avoid_: Docker Desktop, docker (when referring to the CLI command on macOS 26+ with Apple Silicon)
